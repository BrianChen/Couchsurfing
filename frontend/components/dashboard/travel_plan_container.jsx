import { connect } from 'react-redux';
import TravelPlan from './travel_plan';

const mapStateToProps = () => ({

});

const mapDispatchToProps = () => ({

});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(TravelPlan);
